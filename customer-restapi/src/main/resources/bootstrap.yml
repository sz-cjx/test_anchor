#自定义配置
arb:
  service:
    name: customer-restapi
    port: 8102
  eureka:
    username: panda_admin
    password: Arb!8888
  security:
    username: panda_admin
    password: Arb!8888

eureka:
  instance:
    instance-id: ${arb.service.url}:${arb.service.name}:${arb.service.port}
    hostname: ${arb.service.url}
    ip-address: ${arb.service.url}
    prefer-ip-address: true
  #    lease-renewal-interval-in-seconds: 10
  #    lease-expiration-duration-in-seconds: 30
  client:
    healthcheck: enabled
    service-url:
      defaultZone: http://${arb.eureka.username}:${arb.eureka.password}@${arb.eureka.url}:8901/eureka/
server:
  max-http-header-size: 10240000
  port: ${arb.service.port}
spring:
  application:
    name: ${arb.service.name}
  cloud:
    config:
      label: master
      discovery:
        enabled: true
        service-id: config-server
      fail-fast: true
      retry:
        initial-interval: 1000
        multiplier: 1.1
        max-interval: 2000
        max-attempts: 2
      username: ${arb.security.username}
      password: ${arb.security.password}
  datasource:
    master:
      writer:
        driver-class-name: com.mysql.jdbc.Driver
        url: jdbc:mysql://${arb.database.writer.url}/${arb.database.schema}?charset=utf8mb4&useSSL=false&rewriteBatchedStatements=true&autoReconnect=true
        username: ${arb.database.username}
        password: ${arb.database.password}
        test-on-borrow: true                      # 指明是否从池中取出连接前进行检验，如果检验失败，则从池中取出连接并尝试取出另一个
        test-while-idle: true                     # 指明连接是否被空闲连接回收器（如果有）进行检验，如果检测失败，则连接将被从池中去除
        time-between-eviction-runs-millis: 60000  # 在空闲连接回收器线程运行期间休眠的时间值，姨毫秒为单位。如果设置为非整数，则不运行空闲连接回收器线程
      reader:
        driver-class-name: com.mysql.jdbc.Driver
        url: jdbc:mysql://${arb.database.reader.url}/${arb.database.schema}?charset=utf8mb4&useSSL=false&rewriteBatchedStatements=true&autoReconnect=true
        username: ${arb.database.username}
        password: ${arb.database.password}
        test-on-borrow: true                      # 指明是否从池中取出连接前进行检验，如果检验失败，则从池中取出连接并尝试取出另一个
        test-while-idle: true                     # 指明连接是否被空闲连接回收器（如果有）进行检验，如果检测失败，则连接将被从池中去除
        time-between-eviction-runs-millis: 60000  # 在空闲连接回收器线程运行期间休眠的时间值，姨毫秒为单位。如果设置为非整数，则不运行空闲连接回收器线程
  mvc:
    async:
      request-timeout: -1
  http:
    multipart:
      enabled: true
      file-size-threshold: 0
      max-file-size: 256Mb
      max-request-size: 256Mb
  rabbitmq:
    host: ${arb.rabbitmq.url}
    port: 5672
    username: ${arb.rabbitmq.username}
    password: ${arb.rabbitmq.password}
    publisher-confirms: true
    publisher-returns: true
    listener:
      simple:
        acknowledge-mode: manual
encrypt:
  key: Rj0O9LgL^4@S2gMI
---
spring:
  profiles: local
arb:
  eureka:
    url: localhost
  service:
    url: localhost
---
spring:
  profiles: panda-local
arb:
  eureka:
    url: localhost
  service:
    url: localhost
---
spring:
  profiles: panda-develop
arb:
  eureka:
    url: 18.136.106.145
  service:
    url: 3.0.127.85
---
spring:
  profiles: pandaria-develop
arb:
  eureka:
    url: FRAMEWORK-EUREKA-SERVER.us-west-1.elasticbeanstalk.com
  service:
    url: MICROSERVICE-CUSTOMER-RESTAPI.us-west-1.elasticbeanstalk.com
---
spring:
  profiles: pandaria-bamboo
arb:
  eureka:
    url: FRAMEWORK-EUREKA-SERVER.us-west-1.elasticbeanstalk.com
  service:
    url: MICROSERVICE-CUSTOMER-RESTAPI.us-west-1.elasticbeanstalk.com
---
spring:
  profiles: panda-develop-3
arb:
  eureka:
    url: 18.136.106.145
  service:
    url: 54.251.136.239
---
spring:
  profiles: panda-develop-4
arb:
  eureka:
    url: 18.136.106.145
  service:
    url: 54.169.240.186
---
spring:
  profiles: panda-release
arb:
  eureka:
    url: 18.139.107.142
  service:
    url: 18.140.12.120
---
spring:
  profiles: panda-release-2
arb:
  eureka:
    url: 18.139.107.142
  service:
    url: 18.140.125.205
---
spring:
  profiles: pandaria-release
arb:
  eureka:
    url: eureka-rel.ap-southeast-1.elasticbeanstalk.com
  service:
    url: microservice-customer-rel.ap-southeast-1.elasticbeanstalk.com
