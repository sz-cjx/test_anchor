spring:
  cloud:
    nacos:
      discovery:
        service: ${spring.application.name}
        server-addr: ${szt.nacos.addr}
        namespace: ${szt.nacos.namespace}
        group: ${szt.nacos.group}
#        ip: ${szt.nacos.realip}
#        port: ${szt.nacos.realport}
      config:
        file-extension: yaml
        server-addr: ${szt.nacos.addr}
        name: ${szt.nacos.application-name}
        namespace: ${szt.nacos.namespace}
        group: ${szt.nacos.group}
---
spring:
  config:
    activate:
      on-profile: aze-8-feature
szt:
  nacos:
    addr: 47.108.72.215:8848
    application-name: microservice-customer
    namespace: feature
    group: PAN-4700
---
spring:
  config:
    activate:
      on-profile: aze-8
szt:
  nacos:
    addr: 47.108.72.215:8848
    application-name: microservice-customer
    namespace: AZE-8
    group: aze-8
---
spring:
  config:
    activate:
      on-profile: aze-8-rel
szt:
  nacos:
    addr: 47.108.72.215:8848
    application-name: microservice-customer
    namespace: aze-8-rel
    group: aze-8-rel
---
spring:
  config:
    activate:
      on-profile: aze-8-develop-pigeon
szt:
  nacos:
    addr: 47.108.72.215:8848
    application-name: microservice-customer
    namespace: AZE-8-DEVELOP-PIGEON
    group: DEVELOP-PIGEON
